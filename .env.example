SCOPUS_API_KEY=your_scopus_api_key_here

# ORCID public works are read via https://pub.orcid.org (no API key required).
# Do not put ORCID client secrets in the repo; rotate any secret exposed accidentally.
